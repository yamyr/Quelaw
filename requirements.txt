# QueLaw — hackathon MVP dependencies (target: Python 3.12)
streamlit>=1.59.2
chromadb>=1.5.9
python-dotenv>=1.2.2
anthropic>=0.116.0        # optional at runtime; only used when ANTHROPIC_API_KEY is set
python-docx>=1.2.0       # optional .docx upload support
