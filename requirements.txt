# QueLaw — hackathon MVP dependencies (target: Python 3.12)
streamlit>=1.40
chromadb>=0.5.0
python-dotenv>=1.0
anthropic>=0.40        # optional at runtime; only used when ANTHROPIC_API_KEY is set
python-docx>=1.1       # optional .docx upload support
